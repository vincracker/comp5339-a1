# Copy to .env and fill in. NEVER commit the real .env.
#
# TODO(A): confirm which of these are actually needed once the TfNSW account is
# approved and the augmentation source is chosen, then delete the unused lines.

# --- Task 1: EV charger locations -------------------------------------------
# TfNSW Open Data requires a free account: https://opendata.transport.nsw.gov.au
# TODO(A): find out whether one key can be shared across the group or whether
#          each member needs to register separately.
TFNSW_API_KEY=

# --- Task 3: augmentation source --------------------------------------------
# TODO(D): only one of these, depending on the source the group picks.
OPENCHARGEMAP_API_KEY=
GOOGLE_MAPS_API_KEY=
